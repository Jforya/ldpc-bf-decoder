# 零基础项目入门指南
- user：MAXMAXrispa
- 导出时间：2026/6/12 15:09:26
- 页面：https://lyyclaude.gptshare666.pro/chat/e7ce77fd-8665-4fab-82f8-ee886d1bd91e

## 🙋 用户

带着我，从0基础开始做这个项目

## 🤖 Claude

今天这一步到这里就完成了阶段一的骨架。总结一下我们刚刚一起做了什么，以及它为什么重要：
已完成

验证了基矩阵：40×50、Z=40、展开后 H 为 1600×2000，并发现列重是 3/4 混合（不是任务书说的纯 4）——这决定了阈值策略。
理解并实现了多比特 BF 的四步循环（syndrome → 冲突计数 → 同步翻转 → 终止判断）。
用小实验选定了策略：自适应阈值 T = 列重 − 1，同步翻转，无比特可翻则提前失败，max_iter=50。这套定义要原封不动写进报告，因为 RTL 必须复刻同样的语义。
跑出了 9 个 RBER 点的 FER/UBER/平均迭代数据（C 模型，种子固定可复现）。

给你的练习（下次见面前）

把 bf_ref_model.c 在你自己电脑上编译跑一遍（gcc -O2 -o bf_ref bf_ref_model.c，然后 ./bf_ref base.txt 2000），确认结果和上面一致——种子相同，数字应当一模一样。
读懂 build_graph() 函数：为什么块内第 r 行的 1 落在列 (r+s) mod Z？拿一个 4×4 的小单位阵手算 s=1 的循环移位验证一下。这个寻址公式就是未来 RTL 里 qc_addr_gen 模块的全部内容。
思考题：低 RBER 点（比如 0.005）FER 只有 1.5e-3，2000 帧只见到 3 个错误帧，统计上够不够？（提示：任务书 Q4 说每点至少 10 个失败帧。）算一下 0.005 这个点需要仿真多少帧。

后面的路线

下一次：把帧数加到位、生成正式的两张曲线图，并讨论是否要用 Encoder.zip 里的编码器生成非全零码字（任务书允许全零，但报告里要说明为什么全零等价——线性码 + BSC 对称性）。
然后进入阶段二：从 control_fsm 状态机草图开始画 RTL 架构，一个模块一个模块写，每写完一个就和 C 模型对拍。
最后阶段三综合出 PPA。

有任何一步卡住，或者想让我先把某个概念（比如 QC 循环移位、syndrome 的物理意义）再掰开讲细一点，直接说。

## 🙋 用户

把完整的每个阶段的任务都做一遍

## 🤖 Claude

好，那我们就把三个阶段全部走完。我边做边讲每一步的设计决策。先并行准备工具（RTL 仿真用 iverilog，综合用 yosys 做 FPGA 资源评估），同时升级 C 模型：增加自适应帧数（每个点至少凑够 100 个错误帧，满足任务书 Q4 的统计要求）和测试向量导出模式（给阶段二的 RTL 对拍用）。

## 🤖 Claude

120 帧帧级比对一次全过。再做最严格的逐轮轨迹比对：

## 🤖 Claude

三个阶段全部完成。最终成果一览：
阶段一（算法仿真）
C 黄金模型 bf_sim.c：多比特 BF，自适应阈值 T=dv−1（实验证明优于固定阈值，RBER=0.03 时 FER 从 21%→6%），种子固定可复现
9 个 RBER 点，每点 ≥100 个错误帧（最低点自动跑了 95,429 帧），输出 RBER-FER / RBER-UBER / 平均迭代三张图
阶段二（RTL + 验证）
全并行架构：QC 结构化为 Z 位循环移位 → H 矩阵在硬件中变成纯布线，每时钟周期完成一轮迭代，整帧 ≤52 周期
4 个 Verilog 模块 + 基矩阵自动编译脚本（换码型只需重新生成 qc_params.vh）
一致性验证达到任务书的最严格标准：120/120 帧三项输出全对 + 504 个逐轮迭代快照逐位 diff 为空——RTL 与软件比特级等价，无需任何"可解释差异"
阶段三（综合 PPA）
实测综合：10,701 LUT6 / 4,015 FF（每一个 FF 都能对账）/ 0 BRAM / 0 DSP，最长路径 9 级 LUT，估算 Fmax 80~100 MHz，最坏吞吐 ≈3.8 Gbps
Vivado 和 DC 的完整脚本已写好（含全部四类报告命令和约束），你在有商业工具的机器上一条命令复跑即可拿到正式的时序/功耗报告截图
报告 docx 把方法、数据、分析、任务书 Q1~Q5 的回答全部写齐了；zip 里是可复现的完整工程（README 含逐步复现命令）。
两点提醒：①功耗的精确数值必须用 Vivado/DC 跑出来补进报告 6.1/6.2 节，我留的是分析框架和脚本；②答辩时最可能被追问的是"为什么改用自适应阈值"和"逐轮轨迹怎么证明一致性"——这两处报告里都有实验依据，建议你把 2.3 节和 3.3 节吃透。哪个模块的代码想让我逐行讲一遍，随时说。

## 🙋 用户

我计划在这两周时间内去完成一下，请帮我做一个从0开始做起的规划，越详细越好

## 🤖 Claude

好，这份规划按"每天 4–6 小时、共 14 天"设计。一个重要的使用原则先说在前面：上一轮我做完的整套工程（zip 里的代码和报告）请当作"答案册"而不是"起点"——每天先自己写，卡住超过 1 小时再对照我的版本找差距，这样两周后你才真正拥有这个项目，答辩才扛得住追问。
总览：三个里程碑
时间	里程碑	验收标准
Day 1–5	阶段一完成	自己的 C/MATLAB 模型跑出 9 点 FER 曲线，与我的 fer_curve.csv 量级一致
Day 6–11	阶段二完成	自己的 RTL 通过 ≥100 帧对拍，逐轮轨迹 diff 为空
Day 12–14	阶段三 + 报告	Vivado 综合报告四件套到手，报告初稿完成
第一周：算法与软件模型
Day 1 — 理解问题（理论日，不写代码）
上午：读任务书第 1–3 节 + 我给你的那篇 survey 的 Section II（只看 Gallager BF 和 Generic BF Algorithm 两小节，公式 (1)(6)(7)，其余加权算法跳过，本项目不考）。
下午：手算一个微型例子把概念焊死——取一个 4×8 的小 H 矩阵（自己随便构造，每列 2 个 1），假设全零码字、第 3 位出错，手算 syndrome、每个比特的冲突数、判断哪个比特会被翻转。这一步是整个项目的地基，必须手算，不许跳。
自检问题：① syndrome 某一位为 1 意味着什么？② 为什么出错比特的冲突数倾向于更大？③ "同步翻转"和"逐个翻转"结果为什么可能不同？
产出：一页手写推演笔记（以后写报告第 2 节直接用）。
Day 2 — 基矩阵展开
任务：写程序读入 qc_peg_40_50_invc6dplopt_shift_inv.txt，展开成 1600×2000 的 H（或直接建稀疏连接表）。核心公式：基矩阵元素 s≥0 时，块内第 r 行的 1 位于块内列 (r+s) mod Z。
验收（必须全过）：H 总共 7880 个 1；列重为 3 和 4 混合；行重 4–6。如果数字对不上，先检查移位方向（右移 vs 左移是最常见错误）。
同时建立工程习惯：建 git 仓库，今天第一次 commit。
坑预警：注意列重不是任务书说的纯 4——把这个发现记进笔记，它是 Day 4 阈值实验的伏笔。
Day 3 — 单帧 BF 译码器
任务：实现 bf_decode(y) → (success, iters, x_hat)，固定语义：阈值条件用 ≥、同步翻转、无比特可翻提前失败、max_iter=50。
测试顺序（由易到难）：① 全零输入 → 0 轮成功；② 翻 1 个比特 → 1 轮纠回；③ 翻 5 个比特 → 几轮内纠回；④ 翻 200 个比特 → 应失败。
自检：打印每轮的 syndrome 重量（不满足校验数），成功的帧这个数应单调下降到 0。如果震荡不收敛，九成是把"同步翻转"写成了边算边翻。
产出：能跑通上述 4 个用例的译码函数。
Day 4 — 仿真框架 + 阈值实验
上午：搭 BSC 仿真循环：固定随机种子 → 生成噪声帧 → 译码 → 统计 FER/平均迭代。先用每点 300 帧快速跑通流程。
下午：做阈值对比实验——固定 T=3 vs 自适应 T=dv−1，在 RBER∈{0.02, 0.03, 0.04} 各跑 300 帧。你应该复现出自适应阈值大幅占优的结论（我的数据：0.03 处 FER 21%→6%）。
这个实验表格直接进报告，是你"有自己思考"的最强证据。
自检：为什么列重 3 的比特用 T=3 几乎翻不动？（答案在 Day 2 的伏笔里。）
Day 5 — 正式曲线（阶段一收官）
任务：9 个 RBER 点（0.005–0.10），每点跑到 ≥100 个错误帧（低 RBER 点需要几万到十万帧，C 语言几分钟，MATLAB 会慢，建议用 C 或把内层向量化）。
画图：RBER–FER 和 RBER–UBER 两张，对数纵轴，图上标注 max_iter、阈值策略、帧数（任务书 4.3 的硬性要求）。
验收：与我的曲线对比，各点 FER 在 2 倍以内即正常（随机种子不同有统计涨落）。
顺手做：写"失败判定要包含译成错误码字"的残余检查——很多人漏掉，报告会被挑。
产出：fer_curve.csv + 两张图。第一周结束，阶段一闭环。
第二周：RTL、综合与报告
Day 6 — 纸上架构设计（不写 Verilog）
上午：想清楚 QC 旋转这件事并自己推导一遍：syndrome 块 = 码字块循环右移 s 位再异或；冲突向量 = syndrome 块循环左移 s 位再相加。拿 Z=4 的小例子验证方向。
下午：画三张图（A4 手绘即可，拍照进报告）：① 模块框图（top / syndrome_calc / conflict_flip / FSM）；② FSM 状态转移图（IDLE→RUN→输出，标清三个退出条件的优先级：syndrome 全 0 > 翻满 max_iter > 无比特可翻）；③ 一轮迭代的数据流时序。
同时写好 gen_qc_params.py（或手工方式）：把基矩阵编译成 Verilog 可用的连接常数表。
自检：为什么移位量是常数就意味着"综合后零逻辑成本"？
Day 7 — syndrome_calc
写第一个数据通路模块，并单独建 testbench 验证它：从软件模型导出 10 组 (x, syndrome) 黄金对，RTL 算出的 1600 位 syndrome 必须逐位一致。
坑预警：(xb >> s) | (xb << (Z-s)) 在 s=0 时依赖"移位量≥位宽结果为 0"的 Verilog 语义，确认你的仿真器行为；generate 循环里 localparam 切片取连接表是关键技巧，卡住就看我的 syndrome_calc.v。
Day 8 — conflict_flip
同样模式：写模块 + 单独验证。从软件导出 (syndrome, flip_mask) 黄金对比对。
自检：每比特的冲突数最多是几？加法器需要几位？阈值比较为什么可以是常数比较？
Day 9 — 顶层 FSM 集成
把两个数据通路接进 top，写 FSM。逐条核对与软件的语义映射：成功时 iter_count 等于什么？无翻转失败时呢？翻满 50 轮后要不要复查 syndrome？这三个问题答错任何一个，对拍必挂。
先手工跑 3 帧（无错 / 单错 / 必失败帧），用波形看 x 寄存器逐轮变化是否符合预期。
Day 10 — 对拍 testbench
软件模型加 vectors 导出模式：120 帧混合场景（无错 5、单错 5、定数错 10、BSC 100）+ 黄金输出 + 逐轮轨迹。
写 TB：$readmemb 读入 → 逐帧驱动 → 比对 decoded_bits / success / iter_count 三项 → 导出 RTL 逐轮快照。
坑预警 No.1（最高频的坑）：位序约定。文件每行最左字符对应 bit[N-1] 还是 bit[0]？软件写出和 $readmemb 读入必须用同一约定，先用一个 8 比特玩具用例验证位序再上 2000 位。
Day 11 — 调试到全绿（预留整天）
目标：120/120 帧通过 + diff trace_gold.txt trace_rtl.txt 为空。
调试方法论（按命中率排序）：① 位序反了 → 全部帧第一轮就错；② ≥ 写成 > → 部分帧迭代数差 1 或结果不同；③ iter_count 语义差 1 → 比特全对但计数不对；④ max_iter 后没复查 syndrome → 恰好 50 轮收敛的帧 success 不一致。逐轮轨迹的价值就在这：看第一个分叉出现在哪一轮哪一位，直接定位到模块。
如果提前完成：把对拍帧数加到 500，报告里更好看。
Day 12 — 综合与 PPA
用 Vivado 跑 synth_design，出四件套：report_utilization / report_timing_summary / report_power / report_methodology（我的 tcl 脚本可直接用，改一下器件型号）。没有 Vivado 就先用 yosys，但报告里要说明口径。
分析作业（报告 6.1 的"不能只贴工具输出"）：① 对 FF 数逐项对账（应该≈ 2×2000+少量控制位）；② 找到关键路径属于哪段逻辑；③ 算吞吐率：Fmax ×（平均迭代+2）周期/帧 → 帧率 → bps；④ 如果 WNS<0，写出流水化的改进方案（哪怕不实现）。
自检：为什么 BRAM 和 DSP 都是 0？这是答辩必问题。
Day 13 — 报告
按任务书目录写：码参数表 → 算法定义（黄金语义八条）→ 信道与 RBER → 阈值实验 → 曲线与表 → RTL 架构（贴 Day 6 手绘图）→ 接口说明（说明为什么去掉了 THRESH 参数）→ 验证方法与结果 → PPA 表与分析 → Q1–Q5 回答。
我的报告 docx 是结构参考，但数据、图、表必须换成你自己跑出来的，包括你自己的随机种子和帧数。
Day 14 — 缓冲 + 答辩准备
留给前面任何一天的溢出。如果一切顺利，做答辩演练，把这五个问题各准备 1 分钟口头回答：① 为什么用自适应阈值（拿 Day 4 实验说话）；② 怎么证明 RTL 和软件一致（逐轮轨迹 diff）；③ QC 结构在硬件里体现在哪（旋转=布线，BRAM=0）；④ 同步翻转 vs 逐个翻转的区别；⑤ 如果要提高频率/降低面积分别怎么改。
三条贯穿性的纪律
每天 commit + 写 10 分钟日志（今天做了什么、卡在哪、明天第一件事）——日志最后就是报告的"工作过程"素材。
固定随机种子写进代码第一行注释，任何数字都要能一键复现。
进度红线：Day 5 结束阶段一没闭环、或 Day 11 结束对拍没全绿，立刻来找我，我们现场调，不要自己硬耗。
需要的话我可以把这份规划导出成 Word/Markdown 文件方便打印打勾；每天开工时你也可以直接把当天的代码或波形发给我，我按这个计划陪你逐天推进。

## 🙋 用户

可以

## 🤖 Claude

计划表做好了，打印出来就能用：每天一张任务表，左侧是打勾框，右侧是对应的验收标准或自检问题，开头还放了使用原则、三个里程碑和进度红线，方便随时核对自己有没有偏航。
明天就是 Day 1（理论日）。等你做完那个 4×8 小矩阵的手算练习，可以把推演过程发给我，我帮你检查 syndrome 和冲突数算得对不对——这一步打牢了，后面十三天都会顺很多。
